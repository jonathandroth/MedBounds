chorussell <- function(df, R, perturb = 1e-3, A.obs, A.shp, A.tgt, beta.obs,
                       beta.shp, beta.tgt) {

  lbminus <- rep(NA,R)
  ubminus <- rep(NA,R)

  lbplus <- rep(NA,R)
  ubplus <- rep(NA,R)

  lbvec <- rep(NA,R)
  ubvec <- rep(NA,R)

  CSlb5 <- rep(NA,R)
  CSub5 <- rep(NA,R)

  rej5 <- rep(NA,R)
  
}
